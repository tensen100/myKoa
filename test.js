const obj = {
    name1:111,
    name2:222,
    name3:333,
    name4:444
};

const fn = (a) => {
    return new Promise((resolve, reject) => {
        const arr = obj.keys;

    }).then()
};

const index = async () => {
    const arr = Object.keys(obj);
};
index()
